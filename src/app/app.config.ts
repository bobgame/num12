import { NgToggleConfig } from 'ng-toggle-button'

export const ngToggleConfig: NgToggleConfig = {
  color: {
    unchecked: '#999',
    checked: '#2a82e4',
    disabled: '#ccc',
  },
}

export const appConfig = {
  version: '0.0.3',
  dv: 1,
  hv: 1,
}